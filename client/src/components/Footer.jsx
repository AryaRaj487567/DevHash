const Footer = () => (
  <footer className="footer">
    <div className="footer-inner">
      <span>DevHash — a developer publishing workspace.</span>
      <span>Write in Markdown. Publish when it is ready.</span>
    </div>
  </footer>
);

export default Footer;
